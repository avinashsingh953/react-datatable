const settings={
    search: {enabled: false, searchText: "Search",mode: "TEXT"},
    header: {enabled: true, cssClass: 'text-light'},
    enableSorting : false,
    sortBy : [],
}

export default settings